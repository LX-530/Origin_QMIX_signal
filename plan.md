# 从 QMIX 迁移到级联 DQN 的实施方案

## 结论先行

建议实现的是**论文思想的工程化变体：中央式、自回归联合动作 DQN**。每个环境步依次为 24 个信号选择动作，后一个选择以此前动作作为条件。这样把最多 `4^24` 的联合动作搜索降为最多 `24 x 4 = 96` 个候选动作评分，并删除 QMIX mixing network。

但这**不等于论文原算法的直接复现，也不能保证更容易收敛**。论文处理的是“从候选物品中无重复选出 k 个物品”；本项目是“为 k 个不同信号各选一个动作”，不同信号可以选相同编号。准确名称应为 **CDQN-style autoregressive DQN**。

迁移前必须先修正当前训练设置中的两个明显问题：

1. `anneal_steps = 1` 使 epsilon 从 `1.0` 一轮后直接降到 `0.2`。若 24 个智能体各自以 0.2 概率走随机分支，则每步至少一个智能体触发随机分支的概率约为 `1 - 0.8^24 = 99.53%`。
2. 每轮收集 `28 x 150 = 4200` 个环境步，却只有 `train_steps = 1` 次参数更新，更新/数据比过低。

这两项本身就足以造成“24 智能体不收敛”。应保留一个修正后的 QMIX 基线，否则无法判断收益来自算法还是训练配置。

## 论文中真正需要的部分

论文 `1812.10613v3.pdf` 的主题是推荐系统用户模型，级联 DQN 位于第 5 节和附录算法 2：

- 用 `Q1, ..., Qk` 依次选择组合动作的第 1 到第 k 个元素。
- 第 j 个 Q 函数以状态和已选择的动作前缀为条件。
- 所有 `Qj` 拟合同一个 TD 目标：`y = r + gamma * Qk_target(s_next, a_next_1:k)`。
- 使用经验回放和 epsilon 探索，搜索复杂度由组合级降为线性级。
- 论文没有给出足以直接复用到本项目的学习率、batch size 等完整超参数。
- 论文的 GAN 用户模型无需迁移；本项目已有 CTM 仿真环境和显式奖励。

## 当前代码审计

以当前工作区代码为准：

| 项目 | 当前实现 | 需要注意 |
|---|---|---|
| 智能体数 | `utils/CTM/ctm_start/__init__.py` 中是 **18** 个 | 用户所述 24 智能体版本不在当前代码中，24 个 ID、动作映射和 mask 尚拿不准 |
| 元胞 | 71 个，其中末 3 个为出口 | 奖励使用前 68 个非出口元胞 |
| 动作 | 每个信号编号 `0..3`，再映射到方向 `1..4` | 当前多数信号只有 2 个有效动作，1 个信号有 3 个；必须始终使用 mask |
| 全局状态 | 实际为 72 维：总人数 + 71 个混合代价值 | 注释中的 143 已过时；三个含义不同的量被相加，存在状态混叠 |
| 局部观测 | 实际为 136 维：68 个人数 + 68 个火灾风险 | 所有智能体拿到完全相同的观测，并不是真正局部观测；注释中的 25 已过时 |
| 奖励 | `-(静态场 + 火灾暴露 + 拥堵)` | 三项未归一化、无权重，绝对尺度随人数变化 |
| 终止 | 人数为 0 且步数大于 20，或 rollout 达到 150 步 | 环境内人数更新后延迟一拍判断终止，建议修正并测试 |
| 初始人群 | 固定种子生成，总计划人数当前为 217 | 场景变化不足，容易只记住单一场景 |

当前奖励三项为：

- 静态场：`sum(N_i * energy_i / 17)`。
- 火灾：`sum(N_i * fire_risk_i)`，其中 `fire_risk_i` 在 `[0, 1]`。
- 拥堵：密度超过 `2.5` 后，`sum(N_i * (density_i - 2.5) / 3.5)`。

## 建议的 MDP 定义

### 状态

首版建议使用一个明确、归一化的 138 维全局状态：

```text
s_t = [68 个元胞占用率, 68 个元胞火灾风险, 剩余人数比例, 时间比例]

占用率_i = clip(N_i / (6 * area_i), 0, 1)
火灾风险_i = clip(fire_risk_i, 0, 1)
剩余人数比例 = current_people / P_ref
时间比例 = step / 150
```

其中 `P_ref` 取该场景计划进入的总人数，当前场景为 217。拥堵可由占用率推导；静态势能和元胞面积在固定地图中属于常量，不必重复塞入状态。

为隔离变量，第一组消融实验仍应使用“归一化后的现有 72 维 state”；若其效果明显差于 138 维状态，再采用 138 维作为正式版本。

### 动作

环境动作保持不变：

```text
A_t = (a_1, ..., a_24),  a_j in {0, 1, 2, 3}
```

级联过程按固定顺序遍历信号：

```text
a_j = argmax Q_j(s_t, a_1, ..., a_(j-1), candidate)
```

首版沿用 `agent_cell_ids` 的既有顺序，不同时改拓扑。无效动作在 argmax 前置为负无穷；与推荐论文不同，**不能去除其他信号已使用过的动作编号**。

### 奖励

先保留现有目标，只做归一化和显式加权。定义：

```text
E_t = sum(N_i * energy_i / 17) / P_ref
F_t = sum(N_i * fire_risk_i) / P_ref
C_t = sum(N_i * congestion_i) / P_ref

r_t = -(0.4 * E_t + 0.4 * F_t + 0.2 * C_t)
```

理由：范围大致稳定、每步都有反馈、仍同时优化疏散进度、火灾暴露和拥堵。负的逐步代价本身已经鼓励尽快结束，首版不要再叠加终点奖励、差分奖励或复杂 shaping。

`0.4/0.4/0.2` 只是初值，**拿不准**，因为目前没有明确的安全目标权重。若“人身安全优先于疏散时间”是硬要求，优先试 `0.3/0.55/0.15`。最终选择必须同时报告疏散时间、累计火灾暴露和累计拥堵，不能只看训练 reward。

## 网络与训练

### 最小网络

不建立 24 套完全独立网络。使用一个共享的级联网络：

1. 状态编码器：`state_dim -> 128 -> 128`，ReLU + LayerNorm。
2. 信号位置 embedding：16 维。
3. 级联解码器：128 维 GRUCell，隐状态携带动作前缀。
4. Q head：`128 -> 64 -> 4`，输出当前信号四个动作的 Q 值。
5. online/target 两份网络；不同信号共享参数，但由位置 embedding 区分。

这比论文的 24 个独立 `Qj` 更节制，也能延续当前 QMIX 中参数共享的经验。若发现强烈的顺序/位置差异，再增加 24 个轻量独立 head，不先增加复杂度。

### TD 目标

采用 Double DQN，降低 24 级 max 带来的高估：

```text
a_next = CascadeArgmax(Q_online, s_next, next_masks)
y = r + gamma * (1 - done) * Q_target_k(s_next, a_next_1:k)
loss = mean_j SmoothL1(Q_online_j(s, a_1:j), stop_gradient(y))
```

所有级联位置使用同一个 `y`，与论文核心约束一致。经验回放改为 transition 级，不再为时间序列 RNN 保存整段 150 步 episode。

### 第一组超参数

| 参数 | 初值 | 备注 |
|---|---:|---|
| optimizer | Adam | 比当前 RMSprop 更易调 |
| learning rate | **`1e-4`** | 首选；若震荡试 `5e-5`，若稳定但过慢试 `3e-4` |
| gamma | `0.99` | 先与当前工程一致 |
| batch size | `256` transitions | 不是 256 个完整 episode |
| replay capacity | `100000` transitions | 约 666 个满长 episode |
| random warm-up | `5000` transitions | warm-up 后再反向传播 |
| epsilon | `1.0 -> 0.05` | 按 200000 个环境步线性退火 |
| 探索粒度 | 每个联合动作只抛一次 epsilon | 探索时整组动作按 mask 随机生成，避免 24 次概率叠乘 |
| hidden size | `128` | 首版不扫更大网络 |
| target update | 每 1000 次梯度更新硬同步 | 简单、可复现 |
| gradient clip | `10` | 沿用当前值 |
| update/data ratio | `0.25` | 每新增 4 个 transition 更新 1 次；稳定后可升到 1.0 |
| loss | SmoothL1/Huber | 比纯平方误差抗异常 TD 值 |

学习率及训练预算是**初步值，不是论文给定值**。最低只扫三个学习率：`5e-5, 1e-4, 3e-4`，其余先固定。

## 代码迁移清单

1. 新增 `MARL/network/cascade_dqn_net.py`：状态编码、位置 embedding、GRU 级联和 masked Q。
2. 新增 `MARL/policy/cascade_dqn.py`：joint action、Double-DQN target、loss、target 同步和保存加载。
3. 新增 transition replay buffer，字段仅保留 `s, action_vector, reward, s_next, masks, next_masks, done`。
4. 修改 `MARL/agent/agent.py`：加入 `cascade_dqn` 分支和 `choose_joint_action()`；不再逐智能体调用 `choose_action()`。
5. 修改 `MARL/common/rollout.py`：每个环境步一次性取得 24 维动作向量；环境 `step(actions)` 无需重写。
6. 修改 `MARL/runner.py`：按新增 transition 数安排梯度步数，记录 epsilon、TD loss、Q 均值和梯度范数。
7. 修改 `MARL/common/arguments.py`：加入上述超参数，令 `--alg cascade_dqn` 可选。
8. 修改 `env/environment.py`：提供 138 维归一化状态、归一化奖励和及时终止判断。
9. 保留原 QMIX 文件用于对照，不立即删除；确认 CDQN 优于修正后的 QMIX 后再清理。

## 实验顺序与验收

按以下顺序推进，前一步不过就不增加下一层复杂度：

1. **环境单测**：24 个动作 mask 正确；同一状态/动作可复现；reward 三分量范围可记录；终止无一拍延迟。
2. **小规模过拟合**：固定一个场景和少量 transition，loss 能显著下降，所选动作 Q 值能拟合目标。
3. **18 智能体验证**：先在当前可见的 18 智能体版本上跑通，比较原 QMIX、修正 QMIX、级联 DQN。
4. **24 智能体验证**：补齐 24 个信号定义后直接训练；只有确实训练困难时才尝试 18 -> 24 的课程学习。
5. **至少 5 个随机种子**：报告中位数和离散程度，不用单次最好曲线作结论。

建议验收指标：

- 完成率：150 步内全部疏散的 episode 比例。
- 疏散步数：仅对完成 episode 统计，并单独报告未完成率。
- 累计火灾暴露：`sum_t sum_i N_i * fire_risk_i`。
- 累计拥堵：`sum_t sum_i N_i * congestion_i`。
- 归一化回报及其 100-episode 移动平均。
- 级联一致性：同一 transition 上各位置 `Q_j(chosen_prefix)` 与共享 TD target 的误差。

## 最可能遇到的问题

1. **算法适配偏差**：本项目不是无重复集合选择，得到的是论文思想的变体，理论结论不能原样照搬。
2. **顺序偏置**：先选的信号影响后续全部决策。首版固定既有顺序；若结果不稳，只比较既有、反向、按距出口排序三种固定顺序。
3. **Q 值高估累积**：24 级贪心选择会放大高估，故首版直接使用 Double DQN、target network 和 Huber loss。
4. **状态混叠**：当前 72 维状态把静态、火灾、拥堵相加；不同物理状态可能变成同一数值。138 维拆分状态是优先修复项。
5. **奖励尺度漂移**：未归一化奖励随人数和场景改变；必须按 `P_ref` 归一化并记录各分量。
6. **场景过少**：当前人群生成固定种子、火灾来自固定 CSV。若只在一个场景训练，收敛可能只是记忆；应划分训练/验证火灾与人群种子。
7. **24 智能体定义缺失**：当前仓库只有 18 个 `agent_cell_ids` 和 18 组信号动作，这是实施 24 智能体版本前唯一必须补齐的外部信息。
8. **运行环境不完整**：当前 Python 环境缺少 `shapely`，尚不能直接实例化 CTM 环境做数值基线；正式改造前先按 `requirements.txt` 建立可运行环境。

## 决策标准

只有当级联 DQN 在相同场景、相同环境步预算和至少 5 个种子下，同时满足以下条件，才替换 QMIX：

- 24 智能体完成率更高或相当；
- 中位疏散时间更短；
- 累计火灾暴露不恶化；
- 训练方差更小。

否则优先保留修正后的 QMIX，因为“换成级联 DQN更容易收敛”目前只是合理假设，不是论文或现有代码已经证明的结论。
